# =====================================================================
# 🐳 docker-compose.yml — qoe.fi monorepo (PRODUCTION)
# =====================================================================
# 📖 14 services (nom de service = sous-domaine quand il existe) :
#   1. caddy      : reverse proxy + TLS (seul service exposé au public)
#   2. tenants    : Next.js blogs créateurs (wildcard *.qoe.fi) — apps/tenants
#   3. hi         : Next.js marketing (hi.qoe.fi) — apps/hi
#   4. core       : Next.js reader + auth (qoe.fi) — apps/core
#   5. studio     : Next.js créateurs (studio.qoe.fi) — apps/studio
#   6. admin      : Next.js plateforme (admin.qoe.fi)
#   7. api        : backend-of-record Go (api.qoe.fi) — apps/api
#   8. worker      : worker asynq Go (webhooks, newsletter, embeddings, collab)
#   9. embedding  : llama.cpp — jina-embeddings-v3 (inférence locale)
#   10. redis     : cache + queue (asynq)
#   11. meilisearch: full-text search (réseau interne uniquement)
#   12. umami     : analytics self-hosted
#   13. umami-db  : PostgreSQL d'umami
#   14. migrate   : one-shot Prisma migrate (s'exécute puis s'arrête)
#
# ⚠️ worker-node (BullMQ Node) a été SUPPRIMÉ : vestige de l'ancienne stack.
#    Plus rien n'enqueue vers BullMQ — tout passe par asynq (worker).
#
# 🔒 Segmentation réseau par rôle :
#   qoefi-public  : caddy + frontends (web/landing/feed/dashboard/admin/umami)
#                   + supabase-kong/supabase-studio (voir l'override Supabase
#                   créé par bootstrap.sh → docker-compose.override.yml).
#                   Caddy n'a PAS accès au réseau supabase (donc pas à la DB).
#   qoefi-private : tout le reste (api, worker, redis, meili, embedding).
#   supabase_default : UNIQUEMENT api, worker et migrate
#                      (accès à la DB Supabase). Pas les frontends, pas caddy.
#
# 📖 Convention de nommage : kebab-case, préfixe qoefi-, nom de service = nom
#    de conteneur. Le DNS interne Docker utilise le NOM DE SERVICE (pas le
#    container_name) — les deux restent donc synchronisés.
# =====================================================================

name: qoefi

services:
  # ───────────────────────────────────────────────────────────────────
  # 🚪 CADDY : reverse proxy + TLS (ports publics)
  # ───────────────────────────────────────────────────────────────────
  caddy:
    image: caddy:2-alpine
    container_name: qoefi-caddy
    restart: unless-stopped

    # Écoute publique
    ports:
      - '80:80'
      - '443:443'
      - '443:443/udp'

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    volumes:
      - ./docker/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
      - /etc/letsencrypt:/etc/letsencrypt:ro

    # Caddy a besoin de connaître les autres services
    depends_on:
      tenants:
        condition: service_started
      core:
        condition: service_started
      studio:
        condition: service_started
      admin:
        condition: service_started
      hi:
        condition: service_started

    # 🔒 Réseau PUBLIC uniquement : atteint les frontends (qoefi-public)
    #    et kong/studio (via le réseau qoefi-public partagé par l'override
    #    Supabase). Jamais le réseau supabase_default → pas d'accès à la DB.
    networks:
      - qoefi-public

    # Resource limits
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # 🌐 TENANTS : Next.js blogs créateurs (wildcard *.qoe.fi) — apps/tenants
  # ───────────────────────────────────────────────────────────────────
  tenants:
    build:
      context: .
      dockerfile: Dockerfile
      target: tenants
    image: qoefi-tenants:latest
    # Les frontends Next.js font du Prisma direct vers supabase-db : ils
    # doivent être sur supabase_default (comme api/worker/migrate).
    networks:
      - qoefi-public
      - qoefi-private
      - supabase_default
    container_name: qoefi-tenants
    restart: unless-stopped

    # PAS de port public : seul Caddy y accède
    expose:
      - '3000'

    env_file:
      - .env.docker

    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      # URLs publiques (utilisées pour les redirects, emails, etc.)
      NEXT_PUBLIC_APP_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_CONSOLE_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_LANDING_URL: https://hi.${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_API_URL: https://api.${PRIMARY_DOMAIN:-qoe.fi}
      # Backend-of-record Go (réseau privé Docker, jamais public).
      QOE_API_URL: http://api:8080
      # Supabase (lu côté serveur via l'app)
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    depends_on:
      migrate:
        condition: service_completed_successfully

    deploy:
      resources:
        limits:
          memory: 1024M
          cpus: '1.5'
        reservations:
          memory: 256M

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # 📣 HI : Next.js marketing (hi.qoe.fi) — apps/hi
  # ───────────────────────────────────────────────────────────────────
  hi:
    build:
      context: .
      dockerfile: Dockerfile
      target: hi
    image: qoefi-hi:latest
    container_name: qoefi-hi
    restart: unless-stopped

    expose:
      - '3000'

    env_file:
      - .env.docker

    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_PUBLIC_APP_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_CONSOLE_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_LANDING_URL: https://hi.${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_API_URL: https://api.${PRIMARY_DOMAIN:-qoe.fi}
      # Backend-of-record Go (réseau privé Docker, jamais public).
      QOE_API_URL: http://api:8080
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    # Frontend Next.js : Prisma direct vers supabase-db → supabase_default requis.
    networks:
      - qoefi-public
      - qoefi-private
      - supabase_default

    depends_on:
      migrate:
        condition: service_completed_successfully

    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 128M

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # 📰 CORE : Next.js public reader + auth (qoe.fi / www.qoe.fi) — apps/core
  # ───────────────────────────────────────────────────────────────────
  core:
    build:
      context: .
      dockerfile: Dockerfile
      target: core
    image: qoefi-core:latest
    container_name: qoefi-core
    restart: unless-stopped

    expose:
      - '3000'

    env_file:
      - .env.docker

    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_PUBLIC_APP_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_CONSOLE_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_LANDING_URL: https://hi.${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_API_URL: https://api.${PRIMARY_DOMAIN:-qoe.fi}
      # Backend-of-record Go (réseau privé Docker, jamais public).
      QOE_API_URL: http://api:8080
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    # Frontend Next.js : Prisma direct vers supabase-db → supabase_default requis.
    networks:
      - qoefi-public
      - qoefi-private
      - supabase_default

    depends_on:
      migrate:
        condition: service_completed_successfully

    deploy:
      resources:
        limits:
          memory: 768M
          cpus: '1.0'
        reservations:
          memory: 128M

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # 🎨 STUDIO : Next.js creator (studio.qoe.fi) — apps/studio
  # ───────────────────────────────────────────────────────────────────
  studio:
    build:
      context: .
      dockerfile: Dockerfile
      target: studio
    image: qoefi-studio:latest
    container_name: qoefi-studio
    restart: unless-stopped

    expose:
      - '3000'

    env_file:
      - .env.docker

    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_PUBLIC_APP_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_CONSOLE_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_LANDING_URL: https://hi.${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_API_URL: https://api.${PRIMARY_DOMAIN:-qoe.fi}
      # Backend-of-record Go (réseau privé Docker, jamais public).
      QOE_API_URL: http://api:8080
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    # Frontend Next.js : Prisma direct vers supabase-db → supabase_default requis.
    networks:
      - qoefi-public
      - qoefi-private
      - supabase_default

    depends_on:
      migrate:
        condition: service_completed_successfully

    deploy:
      resources:
        limits:
          memory: 768M
          cpus: '1.0'
        reservations:
          memory: 128M

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # 🛡️ ADMIN : Next.js platform admin (admin.qoe.fi)
  # ───────────────────────────────────────────────────────────────────
  admin:
    build:
      context: .
      dockerfile: Dockerfile
      target: admin
    image: qoefi-admin:latest
    container_name: qoefi-admin
    restart: unless-stopped

    expose:
      - '3000'

    env_file:
      - .env.docker

    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_PUBLIC_APP_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_CONSOLE_URL: https://${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_LANDING_URL: https://hi.${PRIMARY_DOMAIN:-qoe.fi}
      NEXT_PUBLIC_API_URL: https://api.${PRIMARY_DOMAIN:-qoe.fi}
      # Backend-of-record Go (réseau privé Docker, jamais public).
      QOE_API_URL: http://api:8080
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    # Frontend Next.js : Prisma direct vers supabase-db → supabase_default requis.
    networks:
      - qoefi-public
      - qoefi-private
      - supabase_default

    depends_on:
      migrate:
        condition: service_completed_successfully

    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 128M

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # ⚡ API (backend-of-record Go) : serveur REST — apps/api
  # ───────────────────────────────────────────────────────────────────
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    image: qoefi-api:latest
    container_name: qoefi-api
    restart: unless-stopped

    expose:
      - '8080'

    env_file:
      - .env.docker

    environment:
      PORT: 8080
      API_PORT: 8080
      REDIS_URL: redis://redis:6379
      QOE_INTERNAL_SECRET: ${QOE_INTERNAL_SECRET:-}
      # Umami self-hosted (réseau Docker). L'API REST v2 est sous /api (vs /v1 cloud).
      UMAMI_API_URL: http://umami:3000/api
      # Auth : soit UMAMI_API_KEY (cloud), soit UMAMI_USERNAME/UMAMI_PASSWORD (self-hosted v2).
      UMAMI_API_KEY: ${UMAMI_API_KEY:-}
      UMAMI_USERNAME: ${UMAMI_USERNAME:-}
      UMAMI_PASSWORD: ${UMAMI_PASSWORD:-}
      NEXT_PUBLIC_UMAMI_WEBSITE_ID: ${NEXT_PUBLIC_UMAMI_WEBSITE_ID:-}
      # 🧠 Inférence embeddings auto-hébergée (service « embedding » ci-dessous).
      EMBEDDING_URL: ${EMBEDDING_URL:-http://embedding:80}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-jina-embeddings-v3}
      # Tâches typées jina-embeddings-v3 (TEI) — vide = champ omis (llama.cpp).
      EMBEDDING_INDEX_TASK: ${EMBEDDING_INDEX_TASK:-retrieval.passage}
      EMBEDDING_QUERY_TASK: ${EMBEDDING_QUERY_TASK:-retrieval.query}
      # Meilisearch (réseau Docker) — full-text search.
      MEILISEARCH_HOST: http://meilisearch:7700
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:-qoe_master_key_123}

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    # api doit être joignable par Caddy (qoefi-public) ET avoir accès à la DB
    # (supabase_default) et au réseau interne (qoefi-private).
    networks:
      - qoefi-public
      - qoefi-private
      - supabase_default

    depends_on:
      redis:
        condition: service_started

    deploy:
      resources:
        limits:
          memory: 96M
          cpus: '0.5'

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # ⚙️ WORKER (asynq) : webhooks + newsletter + embeddings + collab
  # ───────────────────────────────────────────────────────────────────
  worker:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    image: qoefi-worker:latest
    container_name: qoefi-worker
    restart: unless-stopped
    entrypoint: ['/usr/local/bin/qoe-worker']

    env_file:
      - .env.docker

    environment:
      REDIS_URL: redis://redis:6379
      # 🧠 Inférence embeddings auto-hébergée (même config que l'API).
      EMBEDDING_URL: ${EMBEDDING_URL:-http://embedding:80}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-jina-embeddings-v3}
      EMBEDDING_INDEX_TASK: ${EMBEDDING_INDEX_TASK:-retrieval.passage}
      # Meilisearch (réseau Docker) — full-text search.
      MEILISEARCH_HOST: http://meilisearch:7700
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:-qoe_master_key_123}

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    networks:
      - qoefi-private
      - supabase_default

    depends_on:
      redis:
        condition: service_started

    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.5'

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # 🧠 EMBEDDING (llama.cpp) — Inférence jina-embeddings-v3 auto-hébergée
  # ───────────────────────────────────────────────────────────────────
  # 📖 llama.cpp sert jina-embeddings-v3 (GGUF Q8_0) avec une API
  #    OpenAI-compatible (`/v1/embeddings`, 1024 dims, MRL tronqué à 512
  #    côté client Go). Consommé par l'API Go (`/search/semantic`) et le
  #    worker asynq.
  #    ⚠️ Le GGUF (~600 Mo) doit être placé dans `./models/` sur le host
  #    (téléchargé depuis HF : jina-embeddings-v3-Q8_0.gguf).
  #    ⚠️ Licence : jina-embeddings-v3 est CC BY-NC 4.0 (usage non
  #    commercial uniquement) — à valider pour la prod.
  # ───────────────────────────────────────────────────────────────────
  embedding:
    image: ghcr.io/ggml-org/llama.cpp:server
    container_name: qoefi-embedding
    restart: unless-stopped

    expose:
      - '80'

    command: >
      -m /models/jina-embeddings-v3-Q8_0.gguf
      --embedding
      --pooling mean
      --port 80
      --host 0.0.0.0
      --ctx-size 8192
      --alias jina-embeddings-v3

    volumes:
      - ./models:/models:ro

    networks:
      - qoefi-private

    healthcheck:
      # L'image embarque un healthcheck sur :8080 ; notre serveur écoute :80.
      test: ['CMD', 'curl', '-f', 'http://localhost:80/health']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '2.0'

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  # ───────────────────────────────────────────────────────────────────
  # 🔄 REDIS : cache + queue (RÉSEAU PRIVÉ)
  # ───────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: qoefi-redis
    restart: unless-stopped

    expose:
      - '6379'

    command: >
      redis-server
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
      --appendonly yes
      --appendfsync everysec

    volumes:
      - redis_data:/data
      - ./docker/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro

    networks:
      - qoefi-private

    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 3

    deploy:
      resources:
        limits:
          memory: 384M
          cpus: '0.5'

  # ───────────────────────────────────────────────────────────────────
  # 🔍 MEILISEARCH : Moteur de recherche (RÉSEAU PRIVÉ, aucun port public)
  # ───────────────────────────────────────────────────────────────────
  meilisearch:
    image: getmeili/meilisearch:v1.12
    container_name: qoefi-meilisearch
    restart: unless-stopped
    expose:
      - '7700'
    environment:
      - MEILI_MASTER_KEY=${MEILI_MASTER_KEY:-qoe_master_key_123}
      - MEILI_NO_ANALYTICS=true
    volumes:
      - meilisearch_data:/meili_data
    networks:
      - qoefi-private

  # ───────────────────────────────────────────────────────────────────
  # 📊 UMAMI : Analytics self-hosted (cookie-less, gratuit)
  # ───────────────────────────────────────────────────────────────────
  umami-db:
    image: postgres:16-alpine
    container_name: qoefi-umami-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: umami
      POSTGRES_USER: ${UMAMI_DB_USER:-umami}
      POSTGRES_PASSWORD: ${UMAMI_DB_PASSWORD:-umami}
    volumes:
      - umami_db_data:/var/lib/postgresql/data
    networks:
      - qoefi-private
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${UMAMI_DB_USER:-umami} -d umami']
      interval: 10s
      timeout: 3s
      retries: 5

  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    container_name: qoefi-umami
    restart: unless-stopped
    expose:
      - '3000'
    environment:
      DATABASE_URL: postgresql://${UMAMI_DB_USER:-umami}:${UMAMI_DB_PASSWORD:-umami}@umami-db:5432/umami
      DATABASE_TYPE: postgresql
      HASH_SALT: ${UMAMI_HASH_SALT:-qoe_umami_salt_please_change}
      DISABLE_TELEMETRY: '1'
    depends_on:
      umami-db:
        condition: service_healthy
    networks:
      - qoefi-private
      - qoefi-public
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'

  # ───────────────────────────────────────────────────────────────────
  # 🔄 MIGRATE : one-shot Prisma migrate deploy
  # ───────────────────────────────────────────────────────────────────
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrate # Stage dédié avec schema Prisma + migrations
    container_name: qoefi-migrate
    restart: 'no' # S'exécute une seule fois, ne redémarre pas

    env_file:
      - .env.docker

    # Les variables d'environnement DATABASE_URL et DIRECT_URL sont héritées directement du fichier .env.docker (Supabase Pooler)

    extra_hosts:
      - 'host.docker.internal:host-gateway'

    # 🔒 Réseau DB uniquement — rien d'autre à joindre
    networks:
      - supabase_default

# =====================================================================
# 🌐 RÉSEAUX
# =====================================================================
# qoefi-public   : caddy + frontends + kong/studio (exposé via Caddy)
# qoefi-private  : api/worker/redis/meili/embedding (interne)
# supabase_default : externe (créé par le stack Supabase) — DB uniquement
# =====================================================================
networks:
  qoefi-public:
    name: qoefi-public
    driver: bridge
  qoefi-private:
    name: qoefi-private
    driver: bridge
    internal: false # Doit pouvoir downloader (npm, etc.) au build
  supabase_default:
    external: true

# =====================================================================
# 📦 VOLUMES
# =====================================================================
volumes:
  redis_data:
    name: qoefi-redis-data
  caddy_data:
    name: qoefi-caddy-data
  caddy_config:
    name: qoefi-caddy-config
  meilisearch_data:
    name: qoefi-meilisearch-data
  umami_db_data:
    name: qoefi-umami-db-data
